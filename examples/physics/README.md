# physics-example-project

Physics-domain architecture sketch used to exercise `pypl`. Run from the repo
root:

```
uv run pypl class physics --out diagrams/
uv run pypl seq examples/physics-example-project/main.py --package physics --out diagrams/
```
